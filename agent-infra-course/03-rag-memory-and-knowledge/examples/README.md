# 03 RAG、Memory 与 Knowledge Examples

    这里放的是知识点级别的可运行示例。每个目录都能单独安装依赖、运行代码并执行测试。

    ## 示例列表
    - `01-local-retrieval`：演示本地文档检索与 citation 生成。
- `02-short-term-memory`：演示多轮对话中的短期记忆裁剪。

    ## 使用方式
    1. 先按 `steps.md` 的顺序挑一个示例运行。
    2. 先跑测试，再改代码，再重新跑测试。
    3. 把观察到的行为写进 `experiments.md`。
